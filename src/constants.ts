export const ROUTES = {
  home: "/",
  about: "/about",
  now: "/now",
  rss: "/feed",
  archive: "/archive",
} as const;

export const SESSION_TOKEN = "sessionToken";

export const INITIAL_PASSWORD = "to_be_changed";
